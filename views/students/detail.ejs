<!--
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Student Detail</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f4f4f4;
        }

        .container {
            max-width: 800px;
            margin: 20px auto;
            padding: 20px;
            background-color: #fff;
            border-radius: 5px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }

        h2 {
            text-align: center;
            margin-bottom: 20px;
            color: #333;
        }

        p {
            margin-bottom: 10px;
        }

        a {
            display: inline-block;
            text-align: center;
            margin-top: 20px;
            text-decoration: none;
            border-radius: 5px;
            padding: 8px 15px;
            cursor: pointer;
        }

        .btn-edit {
            background-color: #007bff;
            color: #fff;
        }

        .btn-delete {
            background-color: #dc3545;
            color: #fff;
        }

        .btn-back {
            background-color: #28a745;
            color: #fff;
        }

        button:hover, .btn-edit:hover, .btn-delete:hover, .btn-back:hover {
            opacity: 0.8;
        }
    </style>
</head>
<body>
    <div class="container">
        <h2>Student Detail</h2>
        <p>Name: <%= student.name %></p>
        <p>Birthday: <%= student.birthday %></p>
  

        <a href="/students/update/<%= student._id %>" class="btn-edit">Edit</a>
        <form action="/students/delete/<%= student._id %>" method="POST">
            <button type="submit" class="btn-delete" onclick="return confirm('Are you sure you want to delete this student?')">Delete</button>
        </form>
        <a href="/students/index" class="btn-back">Back to List</a>
    </div>
</body>
</html>
-->
<%- include('../layout/header') %>

<div class="col-sm-12 col-xl-12">
    <div class="bg-light rounded h-100 p-4">
        <h6 class="mb-4">Bảng  Chi Tiết Sinh Viên</h6>
        <table class="table">
            <thead>
                <tr>           
                    <th scope="col">Tên Sinh Viên</th>
                    <th scope="col">Hành động</th>
                </tr>
            </thead>
            <tbody>
                    <tr>
                        <td><%= student.name %></td>
                        <td><a href="/students/update/<%= student._id %>" class="btn btn-primary m-2">Chỉnh Sửa</a>
                            <a> <form action="/students/delete/<%= student._id %>" method="POST">
                                    <button type="submit" class="btn btn-danger m-2"  onclick="return confirm('Bạn có muốn xóa sinh viên này?')">Xóa</button>
                                </form>
                            </a> 
                            <a>
                              <a href="/students/index" class="btn btn-warning m-2">Back to List</a>
                            </a>
                        </td>
                           
                       
                    </tr>
            </tbody>
        </table>
    </div>
</div>
<%- include('../layout/footer') %>